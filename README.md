# my-first-repository
Demo: creating my first repository in class

This is a markdown document. As a `README.md` file, it is the first thing visitors to the repo see. 

You cannot code from here. This isn't `R`, but you can write code:

```
library(tidyverse)
ggplot(mtcars, aes(x = wt, y = disp)) +
  geom_point()
```

## Second level header
